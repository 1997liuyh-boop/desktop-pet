{
  "name": "desktop-pet",
  "version": "1.0.0",
  "description": "桌面宠物系统 - VPet 1:1 复刻, Rust + Tauri v2",
  "scripts": {
    "dev:frontend": "npx http-server dist -p 1420 -c-1 --cors",
    "build:frontend": "echo \"static dist, no build step needed\"",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "scan-assets": "node scripts/scan-assets.js",
    "electron:start": "electron .",
    "electron:dev": "electron . --dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}