# 语音合成文本音色设计方案

## 目标

项目语音合成默认切换到 MiMo-V2.5-TTS 的文本音色设计模型，先不使用内置音色列表。

默认音色方向：16 岁少女感的可爱萝莉音，音色清亮甜美，声音轻盈、有亲近感，适合作为活泼可爱的桌面伙伴日常互动声音。

## 模型选择

默认模型使用 `mimo-v2.5-tts-voicedesign`。

选择原因：

- 不依赖 `冰糖`、`茉莉` 等预置 Voice ID。
- 不需要上传音频样本，接入成本低。
- 可以通过自然语言描述稳定表达桌宠音色。
- 当前项目已有 `tts_speak(text)` 播放链路，继续使用非流式 `wav` 返回即可。

暂不使用：

- `mimo-v2.5-tts`：它依赖预置音色，不符合本阶段目标。
- `mimo-v2.5-tts-voiceclone`：它需要音频样本、Base64、MIME 校验、文件选择和隐私提示，改动范围较大。

## 配置设计

现阶段复用已有 TTS 配置结构，避免做迁移文件：

| 字段 | 现阶段含义 |
| --- | --- |
| `endpoint` | TTS API 地址，默认 `https://api.xiaomimimo.com/v1/chat/completions` |
| `model` | 默认 `mimo-v2.5-tts-voicedesign` |
| `voice` | 在文本音色设计模式下表示“音色描述” |
| `style` | 演绎风格，用于控制语速、情绪、尾音和表达方式 |

默认值：

- `voice`：16 岁少女感的可爱萝莉音，音色清亮甜美，声音轻盈、有亲近感，像活泼可爱的桌面伙伴在说话。
- `style`：语速偏轻快，情绪自然灵动，尾音柔和上扬，表达可爱但不过度夸张。

## 请求结构

文本音色设计模式下，请求体采用以下结构：

```json
{
  "model": "mimo-v2.5-tts-voicedesign",
  "messages": [
    {
      "role": "user",
      "content": "音色设定：...\n演绎风格：..."
    },
    {
      "role": "assistant",
      "content": "实际要播报的文本"
    }
  ],
  "audio": {
    "format": "wav",
    "optimize_text_preview": false
  }
}
```

`optimize_text_preview` 默认关闭，避免模型自动润色桌宠台词后改变既有人设和语气。

## 数据流

1. 设置窗口保存 TTS API Key 和 TTS 配置。
2. 前端桌宠调用 `tts_speak(text)`。
3. Tauri 后端读取 TTS Key 和配置。
4. 后端向 MiMo TTS 接口发起请求。
5. 后端返回 base64 WAV。
6. 前端继续沿用现有音频播放链路播放语音。

## 范围边界

本阶段只处理文本音色设计：

- 默认 TTS 模型切换为 `mimo-v2.5-tts-voicedesign`。
- 设置页将“音色”改为“音色描述”。
- 后端在文本音色设计模式下不再传 `audio.voice`。
- 保留旧字段兼容，不新增配置迁移流程。

本阶段不处理：

- 音色复刻文件上传。
- 唱歌 TTS。文档说明 `mimo-v2.5-tts-voicedesign` 不支持唱歌模式。
- 流式 PCM16 播放。当前项目仍使用非流式 WAV，改动最小。

## 后续扩展

后续如果需要支持更多能力，可以增加显式 `mode` 字段：

- `voice_design`：文本音色设计。
- `preset_voice`：预置音色。
- `voice_clone`：音色复刻。

届时再拆分 `voice_prompt`、`voice_id`、`voice_sample` 等字段，避免一个 `voice` 字段承载过多语义。